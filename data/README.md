# Data Directory

This repository tracks only the `data/` folder structure.

Ignored on purpose:
- raw datasets
- caches
- model binaries
- generated intermediate files

Use this structure locally:
- `data/raw/`
- `data/cache/`
- `data/models/`
