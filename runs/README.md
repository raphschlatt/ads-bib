# Runs Directory

This repository tracks only the `runs/` folder structure.

Each local pipeline execution writes run artifacts (data snapshots, plots, logs)
into a timestamped subfolder under `runs/`. These artifacts are intentionally not
committed to git.
